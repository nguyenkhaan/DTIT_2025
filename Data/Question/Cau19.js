export default function() {
  return `

**Sự khác nhau giữa \`std::endl\` và \`‘\\n’\` khi xuống dòng trong console với \`std::cout\` là?**

**Khi nào thì dùng \`endl\`, khi nào thì dùng \`‘\\n’\`?**

`;
}
